#!/usr/bin/env ruby
# frozen_string_literal: true

env = { "MEGATEST_PROGRAM_NAME" => $0 }
if (adapter_index = ARGV.find_index("-a") || ARGV.find_index("--adapter"))
  ARGV.delete_at(adapter_index)
  env["ARCONN"] = ARGV.delete_at(adapter_index)
end

exec(env, "bundle", "exec", "megatest", *ARGV)
